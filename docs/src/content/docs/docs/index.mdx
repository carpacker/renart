---
title: Overview
description: Renart is the all-in-one data pipeline IDE — open source, running entirely inside your git repository.
---

Renart is the all-in-one data pipeline IDE. It combines the editor, the
scheduler, and the freshness monitor in one open-source tool that runs
entirely inside your git repository — your pipeline is plain files, running
on the warehouse you already use.

<img
  src="/docs-media/workspace-overview.webp"
  alt="The Renart workspace: file explorer, SQL editor, pipeline canvas with freshness badges, a data preview, and the asset workbench in one view"
  width="3200"
  height="2000"
/>

## What you get

- **A pipeline canvas** that shows assets, dependencies, and freshness at a
  glance instead of making you reconstruct them from files by hand.
- **An editor that knows your pipeline.** IntelliSense suggests columns from
  upstream assets and validation flags unresolved references while you type.
- **Preview, then materialize.** Inspect any asset with no side effects, and
  materialize it explicitly when you're sure.
- **Schedules without an orchestrator.** Put a pipeline on a per-environment
  cron and Renart runs it — nothing separate to deploy.
- **Notebooks** for ad-hoc analysis, with inline tables and charts, that can
  promote a cell into a real pipeline asset.
- **Freshness you can see.** Renart fingerprints every asset and shows what's
  fresh, edited, stale, or never built, right on the canvas.

Everything Renart does ends up as an ordinary file change in your repository
— diffable, reviewable, revertible.

## Start here

1. [Installation](/docs/installation/)
2. [Quickstart](/docs/quickstart/) — from install to a scheduled pipeline
3. [Concepts](/docs/concepts/) — the model behind the UI
4. [Tour of the interface](/docs/workspace/interface-tour/)
