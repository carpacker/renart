---
title: Installation
description: Install Renart.
---

Install Renart with:

```bash
curl -LsSf getrenart.com/install.sh | sh
```

## Important

Start Renart inside a Git repository.

Renart is designed around git-native pipeline workspaces, and some workspace flows expect that repository context to exist.
