---
title: The asset editor
description: Edit an asset's code and metadata — IntelliSense, validation, previews, and the workbench.
---

Open any asset from the canvas or the Explorer to edit it. The editor is
pipeline-aware: it knows the asset's upstreams, their columns, and their
types, so problems show up while you type instead of in tomorrow's failed
run.

## Write SQL that knows your pipeline

<img
  src="/docs-media/asset-editor.webp"
  alt="The code view: a completion popup suggesting columns from the upstream raw.customers table, with the column's type shown alongside"
  width="2800"
  height="1800"
/>

- **IntelliSense** completes table names from the pipeline and column names
  from the actual upstream assets, with their types.
- **Validation** flags unresolved tables and columns inline as you type.
- **Format SQL** reformats the statement without touching the header.
- Edits **autosave to the file on disk** — the file in your repository is
  always what you see in the editor.

## Preview without side effects

The results panel under the editor follows the selected asset:

- **Inspect** — preview the data the asset produces. Safe: nothing is
  written. If the query is broken you get a quiet "Preview failed" card
  with the engine's error.
- **Materialize** — execute this asset for real and stream the output.
- **Query** — run an ad-hoc query against the same environment.
- **Type check** — column/type problems for the asset, the same checks as
  [`renart type-check`](/docs/reference/cli/) in CI.
- **History** — this asset's part in past runs.

## The workbench

To the right of the editor, the workbench shows the asset's metadata as
editable cards — the same information as the file header, in form view or
raw YAML:

- **Identity** — name, type, owner, description, tags.
- **Materialization** — write behavior (for example view, or table with
  replace).
- **Dependencies** — upstreams inferred from the SQL, plus any added by
  hand.
- **Columns** — names, types, descriptions, and checks; refresh them from
  the asset's definition after the SQL changes.

Changes made in the workbench are written back into the asset file's
header, so a form edit and a text edit are the same change in review.
